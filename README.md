# rsocket-snowone-2021-demo
RSocket Talk Demo [Построение сервисов с RSocket для взаимодействия в реальном времени]

# Server 

Start Main.java

# Client

```bash
cd web && yarn build && yarn serve -s build
```
